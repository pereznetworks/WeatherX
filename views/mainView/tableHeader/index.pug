if tempTypeFahrenheit
  table(id="tableHdr")
    tbody
      tr
        th(id="today")= "TODAY"
        th(id="dayOfWeek")=`${tableHeader.dayOfWeek}`
        th(id="blank")
        th(id="tempHigh")= `High: ${tableHeader.tempHighF}`
          span(id="todayTempType")= `${fahrenheitFont}`
        th(id="tempLow")= `Low: ${tableHeader.tempLowF}`
          span(id="todayTempType")= `${fahrenheitFont}`
else
  table(id="tableHdr")
    tbody
      tr
        th(id="today")= "TODAY"
        th(id="dayOfWeek")=`${tableHeader.dayOfWeek}`
        th(id="blank")
        th(id="tempHigh")=`High: ${tableHeader.tempHighC}`
          span(id="todayTempType")=`${celsiusFont}`
        th(id="tempLow")= `Low: ${tableHeader.tempLowC}`
          span(id="todayTempType")=`${celsiusFont}`
