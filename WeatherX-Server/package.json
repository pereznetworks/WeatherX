{
  "name": "weatherx-server",
  "version": "0.0.1",
  "description": "weatherx-server, using tomtom, forecast.io api",
  "main": "index.js",
  "dependencies": {
    "axios": "^0.18.0",
    "body-parser": "^1.18.2",
    "express": "^4.16.3",
    "helmet": "^3.15.0",
    "mongoose": "^5.0.14"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "mongod --config data/etc/mongod.conf",
    "start": "nodemon --ignore data/ --ignore logs/ --inspect ./index.js",
    "stop": "mongo admin --eval 'db.shutdownServer()'",
    "runMongod": "mongod --config data/etc/mongod.conf",
    "cleanDb": "node ./src/seed-data/cleanDb.js"
  },
  "keywords": [
    "testing",
    "forecast.io",
    "api"
  ],
  "author": "Daniel Perez. Capstone Project. WeatherX Server",
  "license": "MIT",
  "devDependencies": {
    "morgan": "1.9.1",
    "nodemon": "1.18.10"
  }
}
